{% extends 'base-backend.html.twig' %}

{% block title %}Modifier Auteur{% endblock %}

{% block body %}
    <div class="d-flex flex-wrap justify-content-between align-items-center">
        <h1>Modifier l'auteur</h1>
        {{ include('admin_auteur/_delete_form.html.twig') }}
    </div>
    
    {{ include('admin_auteur/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_admin_auteur_index') }}" class="btn btn-success">Modifier</a>


{% endblock %}
